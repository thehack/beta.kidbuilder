<script type="text/javascript" charset="utf-8">
	function setFocus() {
	document.forms[0].elements[0].focus()

	}
	window.onload=setFocus;
	// preload the hooyah
	var hooyah = document.createElement('audio');
	hooyah.setAttribute('src', '/sounds/hooyah.mp3');
	hooyah.load();
</script>
<style>
	.blue {border:solid 2px blue;background-color:lightyellow;}
	.green {border:solid 2px green;background-color:yellowgreen;}
	form input {text-align:center;height:50px;font-size:40px;}
	#verse_text {font-size:48px; line-height:70px;text-align:justify;}
	#begin{font-size:48px; line-height:70px;text-align:center;}
</style>
	<form>
	<p id="verse_text"><%= @verse_layout %>
	</p>
	<p><%= @verse.reference %>
	</p>
	</form>
	<% if @blanks == 0 %>
		<a id="begin" href="/<%= @group.name %>/bbb/<%=h @verse.id %>/easy">Start Now</a>
	<% else %>
		<a id="begin" href="/<%= @group.name %>/bbb/<%=h @verse.id %>/show">PEEK</a>
	<% end %>
<div style="z-index:2;position:absolute;top:100px;font-size:400px;left:50%;color:red;"id="countdown"></div>
<script type="text/javascript" charset="utf-8">

var answers = {};
var wordComplete = function(event) {
	var element = event.target;
	console.log($(element).val());
	$(element).addClass('blue');
	if ( $(element).val().toLowerCase().replace(/^\s+/,"") == element.id.toLowerCase() ) { 
		$(element).toggleClass('blue green');
		answers[element.id.toString()] = 1; 
		if ($(element).next('input') != null) {
			$(element).next('input').focus();}
	};
	if (Object.keys(answers).length == <%= @correct %>) {
		$('#versetext').unbind('keyup');
		hooyah.play();
		$('#verse_text').replaceWith("<%= @success %>");
	if ('<%= @redirect_url %>' == '/profile') {
		$.ajax({ url: '/<%= @verse.id %>/award', type: 'POST'});
	};
		setTimeout(function(){window.location='<%= @redirect_url %>'}, 3000);};
}
$('#verse_text').keyup( wordComplete );

//var seconds = 10;
//if (<%= @blanks %> == 0) {
//	new PeriodicalExecuter(function(pe) {
//		seconds -= 1;
//		$('#countdown').update(seconds);
//		if (seconds <= 0) {pe.stop; window.location = '<%= @redirect_url %>'};
//		} , 1);
//}
//else {
//	$('countdown').hide;
//}

</script>
